import { useState } from "react";
import "./Settings.css";
const options=[['Dark Mode','Use the dark Snapspace appearance',true],['Story Notifications','Know when close friends share',true],['Read Receipts','Let friends know when snaps are opened',false],['Private Profile','Only friends can view your activity',true]];
function Settings(){const [enabled,setEnabled]=useState(options.map((item)=>item[2]));return <main className="settingsPage"><header><p>PREFERENCES</p><h1>Settings</h1></header><section className="settingsCard"><h2>Account & Privacy</h2>{options.map(([title,description],index)=><div className="settingRow" key={title}><span><b>{title}</b><small>{description}</small></span><button className={enabled[index]?"on":""} onClick={()=>setEnabled((items)=>items.map((value,itemIndex)=>itemIndex===index?!value:value))} aria-label={`Toggle ${title}`}><i /></button></div>)}</section><section className="settingsCard"><h2>About</h2><p>Snapchat-inspired college frontend · Version 1.0.0</p></section></main>};export default Settings;
