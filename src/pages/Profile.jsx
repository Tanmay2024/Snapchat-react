import { useState } from "react";
import { FaMedal, FaPen, FaTrophy } from "react-icons/fa";
import "./Profile.css";
function Profile(){const [name,setName]=useState("Admin"),[editing,setEditing]=useState(false),[draft,setDraft]=useState(name);return <main className="profilePage"><header><p>YOUR SNAPSPACE</p><h1>Profile</h1></header><section className="profileLayout"><div><section className="profileHero"><span className="profileAvatar">A</span><div><h2>{name}</h2><p>@admin · admin@snapchat.com</p></div><button onClick={()=>setEditing(true)}><FaPen /> Edit Profile</button></section><section className="profileStats">{[["24,560","Snap Score"],["12","Day Streak"],["48","Friends"]].map(([value,label])=><div key={label}><b>{value}</b><span>{label}</span></div>)}</section><section className="profileSection"><h2>Team Activity</h2>{["Tanmay Guruvugari shared a story","Reached a 12 day streak with Rohith","Pranav saved a Spotlight moment","Santhosh joined a group chat"].map((item,index)=><div className="activityLine" key={item}><b>{item}</b><small>{index+1} day{index?"s":""} ago</small></div>)}</section></div><aside className="profileSection"><h2>Achievements</h2><div className="achievement"><FaTrophy/><span><b>Snap Starter</b><small>Shared 10 moments</small></span></div><div className="achievement"><FaMedal/><span><b>Streak Keeper</b><small>12 days in a row</small></span></div></aside></section>{editing&&<div className="editOverlay"><form onSubmit={(e)=>{e.preventDefault();if(draft.trim())setName(draft.trim());setEditing(false)}}><h2>Edit Profile</h2><label>Display name<input value={draft} onChange={(e)=>setDraft(e.target.value)} /></label><div><button type="button" onClick={()=>setEditing(false)}>Cancel</button><button>Save Changes</button></div></form></div>}</main>};export default Profile;
