import { useState } from "react";
import { FaBookmark, FaHeart, FaShareAlt } from "react-icons/fa";
import "./Spotlight.css";
const posts = [["Study break views", "Rohith", "#e85d75"], ["Built this today", "Pranav", "#3478bf"], ["Weekend ride", "Santhosh", "#f38b37"]];
function Spotlight() { const [category,setCategory]=useState("For You"); const [likes,setLikes]=useState([]); const [saved,setSaved]=useState([]); const toggle=(set,id)=>set((list)=>list.includes(id)?list.filter((item)=>item!==id):[...list,id]); return <main className="spotlightPage"><header><div><p>DISCOVER</p><h1>Spotlight</h1></div><span>Trending moments from the community</span></header><div className="spotCategories">{["For You","Trending","Travel","Campus","Music"].map((item)=><button className={category===item?"active":""} onClick={()=>setCategory(item)} key={item}>{item}</button>)}</div><section className="spotGrid">{posts.map(([title,author,color],index)=><article className="spotCard" key={title}><div className="spotVisual" style={{background:`linear-gradient(145deg, ${color}, #291f3f)`}}><b>{category}</b></div><div className="spotInfo"><h2>{title}</h2><p>by {author} · {24-index*5}.8K views</p><div><button className={likes.includes(index)?"selected":""} onClick={()=>toggle(setLikes,index)}><FaHeart /> {likes.includes(index)?"Liked":"Like"}</button><button className={saved.includes(index)?"selected":""} onClick={()=>toggle(setSaved,index)}><FaBookmark /> Save</button><button onClick={()=>navigator.clipboard?.writeText(window.location.href)}><FaShareAlt /> Share</button></div></div></article>)}</section></main>; }
export default Spotlight;
